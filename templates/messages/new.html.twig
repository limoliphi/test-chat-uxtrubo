{% extends 'layouts/base.html.twig' %}

{% block title 'New Message' %}

{% block body %}
    <h1>New Message</h1>

    <turbo-frame id="new_message">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <br>

            <button type="submit" formnovalidate>Send</button>
        {{ form_end(form) }}
    </turbo-frame>

    <p><a href="{{ path('app_rooms_show', {'id': room.id}) }}">Back</a></p>
{% endblock %}
